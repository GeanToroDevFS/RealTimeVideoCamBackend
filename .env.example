# Server configuration
PORT=10001

# Firebase configuration options (choose one strategy)
FIREBASE_PROJECT_ID=your-project-id
# Paste the full JSON for the service account, keep it in a single line or base64 encode before pasting
FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"your-project-id"}
# Alternatively, point to a JSON file relative to the project root, e.g. ./service-account.json
# FIREBASE_SERVICE_ACCOUNT_PATH=./service-account.json
